# Grid Trading Bot Environment Variables
#
# For Docker deployment:
#   - Copy this file to .env
#   - Fill in your actual values
#   - DB_HOST and REDIS_HOST are overridden by docker-compose.yml
#
# IMPORTANT: Never commit .env to version control!

# =============================================================================
# Exchange API (Binance)
# =============================================================================
# Get your API keys from: https://www.binance.com/en/my/settings/api-management
# IMPORTANT: Enable Futures trading and set IP whitelist for security

BINANCE_API_KEY=your_api_key
BINANCE_API_SECRET=your_api_secret

# Testnet mode: Set to 'true' for testing, 'false' for live trading
# CRITICAL: Double-check this setting before deployment!
BINANCE_TESTNET=true

# =============================================================================
# Database (PostgreSQL)
# =============================================================================
# For Docker: DB_HOST is automatically set to 'postgres' by docker-compose
# For local development: Use 'localhost'

DB_HOST=localhost
DB_PORT=5432
DB_NAME=trading_bot
DB_USER=postgres
DB_PASSWORD=your_password

# =============================================================================
# Redis Cache
# =============================================================================
# For Docker: REDIS_HOST is automatically set to 'redis' by docker-compose
# For local development: Use 'localhost'

REDIS_HOST=localhost
REDIS_PORT=6379

# =============================================================================
# Notifications (Discord)
# =============================================================================
# Create a webhook in your Discord server:
#   Server Settings > Integrations > Webhooks > New Webhook

DISCORD_WEBHOOK_URL=your_webhook_url

# Optional: Discord Bot token (for /bot commands)
# DISCORD_BOT_TOKEN=your_bot_token
# DISCORD_GUILD_ID=your_guild_id
# DISCORD_CHANNEL_ID=your_channel_id

# =============================================================================
# Logging
# =============================================================================
# Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# =============================================================================
# Optional: Monitoring
# =============================================================================
# Prometheus metrics endpoint (uncomment to enable)
# METRICS_PORT=9090

# =============================================================================
# Production Checklist
# =============================================================================
# Before going live, ensure:
#   [ ] BINANCE_TESTNET=false
#   [ ] API keys are for your live account
#   [ ] IP whitelist is configured on Binance
#   [ ] DB_PASSWORD is a strong password
#   [ ] Discord webhook is set up for alerts
